# Cerulean Engine
A graphic engine for Windows and Linux build in C++.


## Requirements
Windows:
    Any compiler with C++ 17 or higher and editor, recommend MSVC (any Visual Studio install)
    cmake

Linux:
    Any compiler with C++ 17 or higher, recommend g++ (with VS Code)
    cmake
    libx11-dev
    xorg-dev
